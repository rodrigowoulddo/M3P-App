export interface Setor{
  key?: string,
  ativo?:boolean,
  sigla: string,
  nome: string,
  nivel?: string,
  vinculo?: string,
  lider?: string,
  emailLider?: string,
  ultimaAvaliacao?: string,
  sendoAvaliado?: boolean,
}
