export interface Setor{
  key?: string,
  sigla: string,
  nome: string,
  nivel?: string,
  vinculo?: string,
  lider?: string,
  emailLider?: string,
  ultimaAvaliacao?: string,
  sendoAvaliado?: boolean
}
