export interface Setor{
  key?: string,
  sigla: string,
  nome: string,
  nivel?: number,
  vinculo?: string,
  lider?: string,
  emailLider?: string,
  ultimaAvaliacao?: string,
  sendoAvaliado?: boolean
}
